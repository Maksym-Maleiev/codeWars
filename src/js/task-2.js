// task -2

function makeNegative(num) {
  if (num > 0) {
    console.log(-(num));
  } else if (num === 0) {
    console.log(num);
  } else {
    console.log(num);
  }

  return num;
}

console.log(makeNegative(1));